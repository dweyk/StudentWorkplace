<Page x:Class="StudentWorkplace.Views.Lectures.LecturesViewingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="LecturesCrudPage"
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding LecturesViewingPageViewModel, 
                    Source={StaticResource ViewModelLocator}}">
    <Grid Background="White">
        <Image Source="/Resources/Image/background4.png" Stretch="UniformToFill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <DataGrid ColumnWidth="*"
                  ItemsSource="{Binding Lectures, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False"
                  Margin="10,60,10,10"
                  SelectedItem="{Binding SelectedLecture}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Заголовок лекции:" Binding="{Binding Title}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Background="Blue" Foreground="White" BorderThickness="0" Content="Прочитать выбранную лекцию" Command="{Binding ReadLectureCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"/>
    </Grid>
</Page>
